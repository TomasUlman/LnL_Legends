'use strict';

class Question {
    constructor(image, coords, name) {
        this.image = image; // String with image src
        this.coords = coords; // Array with coords
        this.name = name; // String
    }
}
